export default function ProfileComponent(props) {
    return (
        <div className={`
            
        `}>
            <h1 className={`
                text-[0.667rem]
                text-black
                
            `}>
                {props.titulo}
            </h1>
        </div>
    )
}